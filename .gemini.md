### 專案概觀

此專案包含使用群益資本 (Capital Securities) 策略王 API 的 Python 範例程式碼。專案展示了如何透過 COM 元件與 API 互動，以實現程式交易功能，例如登入、帳戶資訊查詢、下單、報價接收和回報處理。

專案主要分為兩大部分：
1.  **GUI 互動範例** (`CapitalAPI_2.13.57_PythonExample`): 提供圖形化介面，讓使用者可以手動執行登入、下單、查詢等操作。
2.  **背景資料更新腳本** (`futures_data_updater.py`): 一個獨立的腳本，用於登入 API、根據 `captial_chart` 表中的設定抓取國內外期貨商品的 K 線資料 (支援分 K)，並將其儲存到 MySQL 資料庫 (`captial_kline`) 中。

---

### 重要前置設定

在執行任何 Python 腳本之前，必須先完成以下設定：

1.  **註冊 COM 元件**:
    - 以 **系統管理員** 身分執行以下批次檔，以註冊 API 所需的 DLL 檔案。
    - `CapitalAPI_2.13.57_PythonExample\元件\x64\install.bat`

---

### 專案依賴 (Dependencies)

您需要安裝以下 Python 套件才能執行此專案中的所有功能：

```bash
pip install pywin32
pip install comtypes
pip install pandas
pip install pymysql
```

- `pywin32` 和 `comtypes`: 用於和 Windows COM 元件進行互動。
- `pandas`: 用於處理 `futures_data_updater.py` 中的表格資料。
- `pymysql`: 用於 `futures_data_updater.py` 將資料寫入 MySQL 資料庫。

---

### 主要檔案與執行方式

#### 1. 背景資料更新腳本 (核心功能)

- **檔案**: `futures_data_updater.py`
- **功能**:
    - 自動登入群益 API。
    - 讀取 MySQL 資料庫 `captial_chart` 表中 `enable=1` 的商品。
    - 支援國內 (TAIFEX) 及海外 (CME, CBOT 等) 期貨商品。
    - 針對海外商品使用通用代碼 (如 `CL0000`) 抓取分鐘 K 線 (Type 0)，解決了換月合約代碼問題。
    - 將 K 線資料存入 `captial_kline` 資料表。
- **執行前設定**:
    1.  建立一個 `config.ini` 檔案在專案根目錄。
    2.  在 `config.ini` 中填寫您的 API 帳號密碼以及資料庫連線資訊，格式如下：
        ```ini
        [CAPITAL]
        user_id = YOUR_ID
        password = YOUR_PASSWORD

        [DATABASE]
        host = localhost
        user = root
        password = your_db_password
        db = capital_api_data
        charset = utf8mb4
        ```
- **手動執行指令**:
  ```bash
  python futures_data_updater.py
  ```

#### 2. 自動排程執行

為了方便每日自動更新數據，專案提供了輔助檔案：

- **`run_updater.bat`**: 批次檔，用於設定正確的工作目錄並執行 Python 腳本。
- **`how_to_schedule_task.md`**: 詳細教學文件，指導如何使用 Windows 工作排程器 (Task Scheduler) 設定每日定時任務。

#### 3. GUI 互動範例 (參考用)

- **主要進入點**: `CapitalAPI_2.13.57_PythonExample\PythonExampleV2\Login\LoginForm.py`
- **功能**: 啟動一個 Tkinter GUI 視窗，提供登入、帳號選擇、下單、查詢等功能。
- **執行指令**:
  ```bash
  python CapitalAPI_2.13.57_PythonExample\PythonExampleV2\Login\LoginForm.py
  ```

---

### 資料庫結構簡介

- **`captial_chart`**: 存放目標商品列表，欄位包括 `code` (代碼), `exchange` (交易所), `enable` (是否啟用) 等。
- **`captial_kline`**: 存放 K 線數據，欄位包括 `date`, `code`, `volume`, `open`, `high`, `low`, `close`, `exchange`。
